# Caddy fetches and renews a Let's Encrypt certificate for $DOMAIN automatically and
# reverse-proxies to the Node service. The /updates/* static feed (issue #24 / #11)
# will be added here later; for now it's just the API.
{$DOMAIN} {
	encode zstd gzip
	reverse_proxy api:3000
}
