node_modules/
*.log

# Secrets — never commit key material or env files
.env
.env.*
!.env.example
*.env
*.pem
keys/

# Local database + backups
data/
*.db
*.db-journal
*.db-wal
*.db-shm
backups/

# Terraform — state can contain sensitive attributes; never commit it or real tfvars
.terraform/
.terraform.lock.hcl
*.tfstate
*.tfstate.*
*.tfvars
!*.tfvars.example
crash.log
